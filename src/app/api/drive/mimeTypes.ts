// Drive mimeTypes used to distinguish file kinds stored in the user's appDataFolder,
// so listing one kind (e.g. decks) doesn't pick up files of another kind (e.g. Reports).
export const DECK_MIME_TYPE = 'application/json';
export const REPORT_MIME_TYPE = 'application/vnd.webula.report+json';
