{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "NODE_ENV=test yarn test --ci && yarn build",
  "ignoreCommand": "case \"$VERCEL_GIT_COMMIT_MESSAGE\" in wip:*) exit 0;; *) exit 1;; esac"
}
